#TempConvert.py
TempStr = input("请输入带有符号的温度为：")
if TempStr[-1] in ['c', 'C']:
    F = eval(TempStr[0:-1])*1.8 +32
    print("转换后的温度是{:.2f}F".format(F))
elif TempStr[-1] in ['f', 'F']:
    C = (eval(TempStr[0:-1]) -32)/1.8
    print("转换后的温度是{:.2f}C".format(C))
else:
    print("格式错误")
